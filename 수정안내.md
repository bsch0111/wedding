# 결혼 안내장 — 수정 안내

두 버전 모두 **HTML 파일 하나만 더블클릭하면 열립니다.** (Node·서버 필요 없음)

- `청첩장-밝은버전.html` — 밝고 따뜻한 분위기
- `wedding.html` — 차분하고 깊은 분위기

사진은 `assets/` 안에 **용도별 폴더**로 들어 있어, **HTML 파일과 `assets` 폴더를 같이 두어야** 사진이 보입니다.

## 폴더 구조
```
wedding/
├── 청첩장-밝은버전.html
├── wedding.html
├── result/                원본 고화질 사진 보관
└── assets/
    ├── cover/    📷 표지 사진 1장  (01.jpg)
    ├── journey/  📷 여정 사진 3장  (01·02·03.jpg)
    ├── gallery/  📷 갤러리 사진들  (01·02·03…jpg)
    └── gif/      🎞 GIF
```

자세한 폴더 설명은 `README.md`의 "📷 사진 폴더 안내" 참고.

## 글 · 사진 · 날짜 바꾸기 (차분한 버전)
HTML 파일을 메모장/편집기로 열면 **맨 위에 `CONFIG` 부분**이 있습니다. 여기만 고치면 됩니다.

- **글**: 따옴표 `""` 안 글자만 수정. 줄바꿈은 `\n`.
- **사진**: 각 폴더(`cover`/`journey`/`gallery`)에 사진을 넣고 파일명을 `01.jpg`, `02.jpg`…로 맞춤.
  - 경로 지정: 표지 `cover.image` / 여정 `journey.steps[].image` / 갤러리 `gallery.images[]`
- **GIF**: `assets/gif/`에 넣고 `cover.image: "assets/gif/intro.gif"` 처럼 지정 → 첫 화면이 움직임
- **날짜**: `calendar`의 `year / month / highlightDay`만 바꾸면 달력·요일 자동 계산
- **색감**: 파일 안 `<style>`의 `:root` 색상 값 수정

> 수정 후에는 브라우저에서 **새로고침(⌘R)** 하면 반영됩니다.

## 카톡 등으로 공유하려면
파일을 그대로 보내는 것보다 **인터넷에 올려 링크로 공유**하는 것을 추천합니다.
(GitHub Pages, Netlify 등 무료) — 필요하면 배포를 도와드릴 수 있습니다.
